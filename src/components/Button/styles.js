import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  btn: {
    backgroundColor: '#50c878',
    padding: 15,
    borderRadius: 10,
  },

  btnText: {
    color: '#fff'
  }
})